        <!-- MAIN CONTENT -->
        <section>
            <h2>Dashboard</h2>
        </section>

        <div class="row">
            <div class="col s12 m8" id="recent-posts">
                <h4>Recent Blogs</h4>
                {{#each posts as |post|}}
                    {{> my-post-info post}}
                {{/each}}
            </div>

            <div class="col s12 m4 right-align" id="tools">
                <h4>Options</h4>
                <div class="row">
                    <div class="col s12">
                        <a href="/api/posts/create" id="new-post-button" class="waves-effect waves-light btn"><i
                                class="material-icons left">add</i>New Post</a>

                    </div>
                    <div class="col s12">
                        <a id="logout-button" class="logout-btn waves-effect waves-light btn"><i
                                class="material-icons left">arrow_forward</i>Log
                            Out</a>
                    </div>

                </div>

            </div>
        </div>

<script src="/javascript/dashboard.js"></script>